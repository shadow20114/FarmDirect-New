document.addEventListener("DOMContentLoaded",()=>{const sel=document.getElementById("cropSelect"),run=document.getElementById("runAnalysisBtn"),status=document.getElementById("statusLine");let items=[];const esc=v=>String(v??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#039;"}[m]));const money=v=>`₹${Number(v||0).toLocaleString("en-IN",{maximumFractionDigits:0})}`;async function load(){try{items=await apiGet("/produce/list");sel.innerHTML=items.length?items.map((x,i)=>`<option value="${i}">${esc(x.crop_name)} — ${esc(x.location||"-")} • ${Number(x.quantity||0).toLocaleString("en-IN")} ${esc(x.unit||"Qtl")}</option>`).join(""):"<option value=''>No published produce</option>";const q=new URLSearchParams(location.search).get("crop");if(q){const i=items.findIndex(x=>String(x.crop_name||"").toLowerCase().includes(q.toLowerCase()));if(i>=0)sel.value=String(i)}if(items.length)await analyze();else status.textContent="No published produce available for AI analysis.";}catch(e){status.textContent=e.message||"Could not load produce"}}async function analyze(){const p=items[Number(sel.value)];if(!p)return;status.textContent=`Analyzing ${p.crop_name} in ${p.location||"selected market"}...`;run.disabled=true;try{const baseline=Number(p.expected_price||0);const demandIndex=Math.max(0,Math.min(100,Math.round(50+(Number(p.quantity||0)>0?15:0)+(baseline>0?10:0))));const price=await apiGet("/ai/price-predict",{crop:p.crop_name,baseline,demand:demandIndex,quantity:Number(p.quantity||0)});const demand=await apiPost("/ai/demand",null,{crop_name:p.crop_name,historical_demand:`${Math.max(10,demandIndex-12)},${Math.max(10,demandIndex-6)},${demandIndex}`,current_month:new Date().getMonth()+1,future_periods:4});const avg=Math.max(baseline,Number(price.mandi_baseline||baseline));const predicted=Number(demand.predicted_demand?.[0]??demandIndex);const sell=await apiPost("/ai/selling-recommendation",null,{crop_name:p.crop_name,current_price:baseline,predicted_demand:predicted,average_market_price:avg});const offers=await apiGet(`/offers/produce/${p.id}`);const matches=(offers.offers||[]).filter(o=>o.status!=="REJECTED").sort((a,b)=>Number(b.offered_price||0)-Number(a.offered_price||0));const conf=Math.max(70,Math.min(95,80+Math.round(Number(price.price_change_percentage||0))));document.getElementById("recommendedPrice").innerHTML=`${money(price.recommended_price)} <span class="text-xs text-slate-500">/ ${esc(p.unit||"Qtl")}</span>`;document.getElementById("priceMessage").textContent=`Confidence ${conf}%`;document.getElementById("mandiRate").textContent=`${money(price.mandi_baseline)} / ${p.unit||"Qtl"}`;document.getElementById("aiTargetRate").textContent=`${money(price.recommended_price)} / ${p.unit||"Qtl"}`;document.getElementById("retailRate").textContent=`${money(price.retail_rate)} / ${p.unit||"Qtl"}`;document.getElementById("priceChange").textContent=`${Number(price.price_change_percentage)>=0?"+":""}${Number(price.price_change_percentage||0).toFixed(1)}% vs baseline`;document.getElementById("priceTrend").textContent=Number(price.price_change_percentage)>=0?"AI indicates pricing room above the current baseline.":"AI suggests monitoring the market before selling.";document.getElementById("demandValue").textContent=price.demand_level;document.getElementById("demandMessage").textContent=`Index ${price.demand_index}/100`;document.getElementById("sellValue").textContent=sell.recommendation;document.getElementById("sellMessage").textContent=`Score ${sell.confidence}/100`;document.getElementById("confidenceValue").textContent=`${conf}%`;document.getElementById("buyerMatches").textContent=String(matches.length);document.getElementById("factorListing").textContent=`${Number(p.quantity||0).toLocaleString("en-IN")} ${p.unit||"Qtl"} listed at ${money(baseline)}.`;document.getElementById("factorDemand").textContent=`Demand index ${price.demand_index}/100 with projected demand ${predicted}.`;document.getElementById("factorMarket").textContent=`Baseline ${money(price.mandi_baseline)} and retail estimate ${money(price.retail_rate)}.`;document.getElementById("analysisText").textContent=`For ${p.crop_name}, the analysis combines listing price, quantity and demand signals to estimate a target price and selling window.`;document.getElementById("demandTrend").textContent=price.demand_level;drawForecast(price.demand_index);drawBuyers(matches);document.getElementById("forecastText").textContent=`Projected demand for the next four periods: ${demand.predicted_demand.join(", ")}.`;status.textContent=`AI analysis complete for ${p.crop_name}.`;}catch(e){console.error(e);status.textContent=e.message||"AI analysis failed. Check the backend AI endpoints.";}finally{run.disabled=false;}}function drawForecast(index){const vals=[Math.max(0,index-8),Math.max(0,index-4),index,Math.min(100,index+4),Math.min(100,index+7),Math.min(100,index+9)];const box=document.getElementById("forecastBars");box.innerHTML=vals.map((v,i)=>`<div class="bg-slate-950 border border-slate-800 rounded-xl p-3 text-center"><p class="text-[10px] text-slate-500">P${i+1}</p><p class="text-lg font-bold text-amber-400 mt-1">${v}</p></div>`).join("");}function drawBuyers(rows){const box=document.getElementById("buyerList"),summary=document.getElementById("buyerSummary");summary.textContent=rows.length?`${rows.length} active offer${rows.length>1?"s":""}`:"No active offers";box.innerHTML=rows.length?rows.slice(0,5).map(o=>`<div class="bg-slate-950 border border-slate-800 rounded-xl p-3"><div class="flex justify-between gap-2"><b class="text-xs">${esc(o.buyer_name||("Buyer #"+o.buyer_id))}</b><span class="text-xs text-emerald-400">₹${Number(o.offered_price||0).toLocaleString("en-IN")}/Qtl</span></div><p class="text-[10px] text-slate-500 mt-1">Offer quantity: ${Number(o.quantity||0)} ${esc(o.unit||"Qtl")} • ${esc(o.status||"PENDING")}</p></div>`).join(""):"<p class='text-xs text-slate-500'>No active buyer offers yet. Buyers can use MAKE OFFER from the marketplace.</p>";}sel?.addEventListener("change",analyze);run?.addEventListener("click",analyze);load();});